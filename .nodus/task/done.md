# Сделано

Архив завершённых задач, свежие сверху.

## 12.06.2026 — t2: реальный звук колонка → Nodus ✅
- Полный путь работает на железе: приложение играет в «Nodus Virtual Audio» →
  драйвер пишет звук в кольцо в общей памяти → движок Nodus читает и выводит
  на реальные динамики. Громкость/mute маршрута действуют вживую.
- Полевые находки (3 итерации): ленивое создание секции (на холодной загрузке
  \BaseNamedObjects ещё нет); перезапуск устройства при обновлении (старый .sys
  живёт в памяти); рендерер терял хвосты кусков при полном буфере (хрип);
  буфер 20→50 мс + timeBeginPeriod(1) (таймер Windows ~15.6 мс — рваная подача).
- Инструменты: ring-check.exe (кольцо живо?) и ring-play.exe (движок + фазы
  громкости) — пригодятся для t3/t4.
- Умный install.ps1: сам решает ставить/обновлять/чинить устройство, версия
  штампуется в CI (1.0.<run>.0).

## 11.06.2026 — t1: драйвер проверен на тестовом ноуте ✅
- «Динамики (Nodus Virtual Audio)» появились в списке устройств вывода Windows —
  то, чего старый драйвер не мог добиться в принципе.
- ~2 минуты музыки на виртуальном устройстве, многократное переключение между
  виртуальным и Realtek — стабильно, без BSOD.
- Попутно: install.ps1 падал на русской локали (длинные тире в UTF-8 без BOM
  ломали парсер PowerShell 5.1) — скрипты переведены на чистый ASCII.

## 11.06.2026 — Первый собранный пакет нового драйвера
- Найдены и исправлены 2 ошибки в каркасе (категории фильтров — без них устройство
  было бы невидимым; ограничение версии Windows в INF).
- Починен триггер автосборки: раньше пуш в ветку вообще не запускал CI.
- CI собрал и подписал тестовый пакет: артефакт `nodus_audio-driver-x64-Release`
  (ветка `feat/sysvad-driver`, коммит `324a19f`).

## 10–11.06.2026 — Каркас нового драйвера (на базе примера Microsoft SYSVAD)
- Структура wave + topology, из которой Windows умеет строить аудио-endpoint.
- Звук пока отбрасывается — каркас проверяет только «устройство видно, ничего не падает».

## До 10.06.2026 — Решение списать старый самописный драйвер
- Дважды ронял Windows (BSOD), архитектурно не мог показать устройство в системе.
- Ревью 11.06 подтвердило: чинить дороже, чем переписать. План миграции:
  `.nodus/sysvad_migration_plan.md`.

## Ранее — всё вне драйвера (работает)
- Rust-движок: маршрутизация, mute/volume/pan на маршрут, splitter, mixer, VU-метры.
- Автодетект приложений, интеграция с UI, сохранение/загрузка сцен.
- Fallback: пока драйвера нет, виртуальные устройства эмулируются через VB-Audio/loopback.
