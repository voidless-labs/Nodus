# t11 — Редизайн UI: лёгкий, дружелюбный стиль

**Статус:** 🔵 в работе с 13.06.2026
**Ждёт:** ничего
**Кто делает:** Claude (по явному разрешению на правку визуала) + разработчик (арт-дирекция, приём шагов)

## Утверждённые решения (13.06.2026)
- **Стек: TypeScript + TSX.** Текущий UI — гиперскрипт `h()` в .jsx, без TS; слой
  отрисовки всё равно переписывается, поэтому переходим на TS заодно (типизированный
  мост к Rust ловит рассинхрон invoke/событий; чище для растущего набора нод).
  Логика НЕ выбрасывается — портируется в типизированные модули.
- **Акцент — янтарный `#F5C542`** (кнопка движка, выделение).
- **Hub-нода ДА** — опциональная нода-«пульт» (как «Stream Mix» на референсе) для
  тех, кто хочет явный микшер с настройками. Слова Splitter/Mixer в UI не вводим.
- **Иконки — реальные** (извлечение из .exe через WinAPI, backend); до готовности
  извлекателя — категорийный глиф-заглушка.
- Главный референс: `.nodus/docs/ReDesign/` (general-view, Node-design[-v2],
  graph-line, bottom bar, Engine-start-btn, scene-tab, Lovable рамка+реакция, V0 add).
- Инструкция: `.nodus/docs/ReDesign/redesign-instructions.md` (приоритет над всем).
- Работаем пошагово: каждый шаг — превью разработчику, правки, дальше.

## Подзадачи (план редизайна)

### Фундамент
- [x] **R1. TS/TSX-тулчейн** ✅ 14.06 — tsconfig.json, typescript+@types в devDeps,
      vite plugin-react компилирует .tsx; вход index.html → src/main.tsx → NodusApp.
      Грабля Windows: имя `App.tsx` коллизирует с `app.jsx` (регистронезависимая ФС) →
      корень назван `NodusApp.tsx`. Старые .jsx оставлены для порта логики.
- [x] **R2. Дизайн-токены + фон холста** ✅ 14.06 — `styles/tokens.css` (вся палитра,
      акцент `#F5C542`, цвета типов, тени/свечения, радиусы, шрифт), `styles/base.css`,
      `ui/Canvas.tsx`+css: графит #0E0E10 + точечная сетка 24px + виньетка. Проверено в превью.

### Логика и каркас
- [~] **R3. Порт логики и моста в TS** — фундамент ✅ 14.06: `bridge.ts` (типизированы все
      invoke-команды и события по Rust-контракту: AudioDevice/Process/RoutingGraph/levels),
      `useBackend.ts` (грузит реальные устройства/процессы + live levels + start/stop движка,
      browser-fallback на sample). AddPanel и кнопка движка переведены на реальные данные.
      Осталось (→ R18): live-метры в ноды, сборка/применение RoutingGraph из холста, save/load.
- [x] **R4. BaseNode** ✅ 14.06 — `ui/nodes/NodeCard.tsx`+css, `NodeIcon.tsx`, `types.ts`:
      ярлык-тип над карточкой, карточка, иконка-глиф, имя, вторичная строка, VU-метр,
      слайдер+mute, порты. Состояния active(свечение)/muted/idle. Проверено через inspect
      (зелёный source, голубой output, метр цветом типа). backdrop-filter убран (перф).
      Осталось в R4: состояния selected/compact (добавить при подключении).
- [~] **R5. Типы нод** — source/output/virtual(mic-сток) + **hub «Stream Mix»** ✅ 14.06
      (`ui/nodes/HubNode.tsx`+css: лейбл-входы mic/music/game как реальные порты, выход mix,
      настройки-чипы, метр). Состояния selected/compact ✅. Осталось: fx/logic ноды. Сюда — **t9**.
- [x] **R6. Реакция рамки на курсор** ✅ 14.06 — `.node-glow`: радиальное свечение цветом
      типа у курсора, маска на 1px-кольцо; rAF-троттлинг, только нода под курсором.
- [ ] **R7. Реальные иконки приложений** — backend-извлекатель HICON из .exe + проброс
      в ноды; устройства — категорийные глифы.

### Рёбра и взаимодействие
- [x] **R8. Рёбра** ✅ 14.06 — `ui/Graph.tsx`+css: безье-провода, эндпоинты меряются из
      DOM (.node-port data-node/data-side), пересчёт на resize/смену нод. Состояния
      покой/активное-свечение/muted-пунктир. Резинка при перетягивании — позже (с R3/drag).
- [ ] **R9. Поповер ребра** — клик по ребру: громкость/mute/баланс L-R/удалить.

### Хром экрана
- [x] **R10. Топбар** ✅ 14.06 — `ui/Topbar.tsx`+css: компактный знак + «nodus», вкладка
      сцены пилюлей + «+», `⋯`-меню, акцентная пилюля start↔live. Каркас app-shell →
      колонка (топбар + canvas-area). Без ENGAGE/IDLE. Доделать: меню `⋯`, overflow вкладок.
- [x] **R11. Плавающая кнопка движка** ✅ 14.06 — `ui/EngineButton.tsx`+css: центр-сверху,
      общее состояние `live` с топбаром (проверено: клик в топбаре синхронит обе).
      Одна короткая пульс-анимация точки при включении.
- [x] **R12. Bottom bar** ✅ 14.06 (рев.2) — `ui/BottomBar.tsx`+css: акцентная панель 360px;
      клик по категории → список раскрывается В СЕРЕДИНЕ (между поиском и вкладками), бар растёт
      вверх; поиск ПОДСВЕЧИВАЕТ совпадающие ноды на холсте (is-search-match/dim), остальные тускнеют
      (проброс search → Graph → ноды). Логика добавления — R3.
- [x] **R13. Панель «+ добавить»** ✅ 14.06 (ревизия) — `ui/AddPanel.tsx`+css: акцентная пилюля
      «+ add» снизу-слева со статусом nodes/routes справа (engine убран); по клику широкая панель
      вверх — поиск (как в BottomBar) + секции «playing now»/«devices» с ЦВЕТНЫМИ круглыми
      иконками по типу (зелёный source / синий output / фиолетовый virtual). По V0+эталону владельца.
- [x] **R14. Зум-контролы** ✅ 14.06 — `ui/ZoomControls.tsx`+css: +/−/fit, тихие (transform — позже).
- [x] **R15. Мини-статус** ✅ 14.06 — счётчики nodes/routes теперь в AddPanel (engine убран);
      отдельный MiniStatus удалён.

### Онбординг и пустой холст
- [x] **R16. Пустой холст** ✅ 14.06 — `ui/EmptyCanvas.tsx`+css + `scenes.ts`: призрак
      source→output + подсказка + 3 пресета (stream/discord/headphones), клик собирает граф.
      Граф вынесен в state (NodusApp), сцены — в scenes.ts (buildPreset). Старт — пусто.
- [x] **R17. Модалка вирт. устройства** ✅ 14.06 — `ui/VirtualDeviceModal.tsx`+css: одна фраза,
      две карточки (auto-install / later), тихий skip, спойлер «technical details» (Test Mode/
      подпись). Показывается при старте. Реальная установка — R18. Сюда же ляжет **t7**.

### Сборка и финал
- [ ] **R18. Переподключение функций** — метры, save/load, solo, pan, PTT, автодетект,
      виртуальные устройства; проверить всё на новом UI.
- [ ] **R19. Проход по производительности** — никаких infinite-анимаций; проверка на
      профиле слабой машины (урок N4200).

## Зависимости подзадач
R1 → R2 → R3 → (R4 → R5 → R6, R7) → (R8 → R9) → (R10–R15) → (R16–R17) → R18 → R19.
R7 (иконки) и часть хрома можно параллелить субагентами (backend vs ui).

## Суть простыми словами
Текущий интерфейс ощущается как профессиональный софт (панели, капс, кокпит) —
а цель Nodus в лёгкости и понятности для обычного человека. Нужно переделать
подачу: растворить раму (убрать постоянные боковые панели, контролы — внутрь
нод), одна акцентная кнопка, мягкие карточки-ноды с живым метром и слайдером,
поповер на ребре вместо инспектора, точечная сетка холста, человеческий язык.
Функциональность НЕ меняется — меняется только внешний вид и компоновка.

Бриф со всей спецификацией: `.nodus/docs/ui-redesign-prompt.md` (изначально
писался для Claude Design; теперь это наш рабочий документ).

## Критерий готовности (укрупнённо; детальная разбивка — в «Подзадачи» ниже)
- [ ] Дизайн-токены в CSS-переменных (палитра, радиусы, шрифты, тени/свечения)
- [ ] Нода-карточка: иконка + имя + живой VU-метр + слайдер; все состояния
- [ ] Рёбра + поповер ребра (громкость/mute/баланс) — замена бокового инспектора
- [ ] Боковые панели растворены: «+ Add» (приложения/устройства), мини-статус в углу
- [ ] Пустой холст первого запуска: призрак-подсказка + пресеты сценариев
- [ ] Онбординг виртуального устройства переписан по-человечески
- [ ] Производительность: никаких вечных анимаций в покое (урок N4200)
- [ ] Вся уже подключённая логика сохранена и переподключена (метры, save/load,
      solo, pan, PTT, автодетект, виртуальные устройства)

## Технические заметки
- Сюда естественно вливаются t9 (виртуальный микрофон как сток-нода) и t7
  (уведомление «не слышу систему» — место в новом UI).
- Порядок итераций (из брифа): токены/фон/типографика → нода → рёбра+поповер →
  растворение панелей → empty state. Каждый шаг — скриншот, правки, дальше.
- Зоны: вид/CSS/layout — разработчик; перенос токенов и переподключение
  invoke/событий — Claude (по правилам проекта — с явного разрешения на правку
  визуала, которое для этой задачи уже дано принципиально).
- Контракт UI↔движок (invoke-команды, события) при редизайне НЕ меняется —
  переезжает только расположение контролов, не их вызовы.
